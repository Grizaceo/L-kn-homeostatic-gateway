# L-kn Gateway Configuration

# Engine Configuration
ENGINE_HOST=127.0.0.1
ENGINE_PORT=30000
ENGINE_MODEL=Qwen/Qwen2.5-3B-Instruct-AWQ
ENGINE_MEM_FRACTION=0.85
ENGINE_TIMEOUT=120

# Gateway Configuration
GATEWAY_HOST=127.0.0.1
GATEWAY_PORT=8000
GATEWAY_LOG_LEVEL=INFO

# Homeostatic Parameters
LKN_DECISION_STRATEGY=rules
LKN_ENTROPY_THRESHOLD=0.6
LKN_PROBE_TOP_K=10
LKN_MAX_TOKENS_FLUIDO=150
LKN_ANALYTIC_SYSTEM_PROMPT=Think carefully, verify your assumptions, and reason step-by-step before answering.
# Options: homeostatic | passthrough | always_analitico
LKN_MODE=homeostatic

# Circuit Breaker
CIRCUIT_BREAKER_THRESHOLD=5
CIRCUIT_BREAKER_TIMEOUT=30

# Retry Configuration
MAX_RETRIES=3
RETRY_BACKOFF_FACTOR=2
